import {NextResponse} from 'next/server';export async function POST(request){try{const{textChunk}=await request.json();if(!textChunk||textChunk.trim().length<10){return NextResponse.json({error:"Texte trop court."},{status:400});}const text=textChunk.trim();const words=text.split(/[\s',’]+/).filter(w=>w.length>0);const wordCount=words.length;const sentences=text.split(/[.!?]+/).filter(s=>s.trim().length>0);const sentenceCount=sentences.length||1;const heavyPhrases=[];let shortSentencesCount=0;let longSentencesCount=0;sentences.forEach(sentence=>{const currentWords=sentence.trim().split(/[\s',’]+/).filter(w=>w.length>0);const len=currentWords.length;if(len<=8&&len>0)shortSentencesCount++;if(len>25){longSentencesCount++;if(heavyPhrases.length<4){heavyPhrases.push({text:sentence.trim().substring(0,130)+(sentence.trim().length>130?"...":""),reason:`Surcharge (${len} mots). Accumulation complexe.`,suggestion:"Scindez au niveau des articulations logiques."});}}});const cleanWordsMapped=words.map(w=>w.toLowerCase().replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g,""));const ttr=(new Set(cleanWordsMapped).size/(wordCount||1))*100;const adverbs=(text.match(/\b\w+ment\b/gi)||[]).length;const adverbRatio=(adverbs/(wordCount||1))*100;const weakVerbs=(text.match(/\b(faire|fait|faisant|fais|faisons|faites|font|avoir|a|as|ont|avez|avons|dire|dit|dis|disons|dites|disent|être|est|suis|es|sommes|êtes|sont|aller|va|vais|allons|allez|vont)\b/gi)||[]).length;const actionCoefficient=Math.max(10,Math.min(95,Math.round(75-(weakVerbs/(wordCount||1))*100+(shortSentencesCount*0.8))));const customLexiconDatabase=[{regex:/un silence de mort/i,expr:"un silence de mort",alt:"un calme sépulcral, une absence de vibration"},{regex:/les larmes aux yeux/i,expr:"les larmes aux yeux",alt:"la vue brouillée, le regard brillant"},{regex:/battre la chamade/i,expr:"battre la chamade",alt:"s'emballer sauvagement, s'accélérer"},{regex:/perdu dans ses pensées/i,expr:"perdu dans ses pensées",alt:"le regard ancré dans le vide, absorbé"},{regex:/blanc comme un linge/i,expr:"blanc comme un linge",alt:"le teint livide, les traits blêmes"},{regex:/monter en haut/i,expr:"monter en haut",alt:"monter, gravir"},{regex:/reculer en arrière/i,expr:"reculer en arrière",alt:"reculer, se replier"},{regex:/savoir à l'avance/i,expr:"savoir à l'avance",alt:"savoir, pressentir"},{regex:/collaborer ensemble/i,expr:"collaborer ensemble",alt:"collaborer"}];const clichesDetected=[];customLexiconDatabase.forEach(item=>{if(item.regex.test(text)){clichesDetected.push({expression:item.expr,alternative:item.alt});}});const spacesBeforePunctuation=(text.match(/\s[:;!?]/g)||[]).length;const missingInsecablesCount=Math.max(0,(text.match(/[:;!?]/g)||[]).length-spacesBeforePunctuation);const spaceChars=(text.match(/\s/g)||[]).length;const typographicGrisRatio=(spaceChars/(text.length||1))*100;let paoStatus="Équilibre du miroir de page optimal.";if(typographicGrisRatio<13)paoStatus="Gris typographique trop dense.";if(typographicGrisRatio>19)paoStatus="Gris typographique trop clair.";let rhythmStyle=`Spectre synesthésique équilibré. [PAO: ${paoStatus}]`;if(longSentencesCount/sentenceCount>0.3){rhythmStyle=`Teinte harmonique contemplative. Prose à ondes amples.`;}else if(shortSentencesCount/sentenceCount>0.4){rhythmStyle=`Teinte harmonique incandescente et staccato.`;}let adverbDensityLabel=`Optimale. Orthotypographie: ${missingInsecablesCount} espaces insécables calibrés.`;if(adverbRatio>1.8)adverbDensityLabel=`Pression modérée. Orthotypographie: ${missingInsecablesCount} espaces ajustés.`;if(adverbRatio>3.5)adverbDensityLabel=`Saturation critique. En attente de normalisation de ${missingInsecablesCount} ponctuations double.`;let scoreCalculated=65+(ttr*0.3)-(adverbRatio*4)-(heavyPhrases.length*5)+(shortSentencesCount*0.5);const hookScore=Math.max(15,Math.min(98,Math.round(scoreCalculated)));return NextResponse.json({metrics:{hookScore:hookScore,rhythmStyle:rhythmStyle,adverbDensity:adverbDensityLabel,vocabularyRichness:Math.round(ttr),readingTime:Math.max(1,Math.ceil(wordCount/200)),dynamicCoefficient:actionCoefficient,weakVerbsCount:weakVerbs},clichesDetected:clichesDetected,heavyPhrases:heavyPhrases});}catch(error){return NextResponse.json({error:"Erreur interne d'extraction matricielle."},{status:500});}}
