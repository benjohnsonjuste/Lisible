import {NextResponse} from 'next/server';export async function POST(request){try{const{textChunk}=await request.json();if(!textChunk||textChunk.trim().length<10){return NextResponse.json({error:"Texte trop court."},{status:400});}const text=textChunk.trim();const words=text.split(/[\s',’]+/).filter(w=>w.length>0);const wordCount=words.length;const sentences=text.split(/[.!?]+/).filter(s=>s.trim().length>0);const sentenceCount=sentences.length||1;const heavyPhrases=[];let shortSentences=0;let longSentences=0;sentences.forEach(s=>{const len=s.trim().split(/[\s',’]+/).filter(w=>w.length>0).length;if(len<=8&&len>0)shortSentences++;if(len>25){longSentences++;if(heavyPhrases.length<4){heavyPhrases.push({text:s.trim().substring(0,130)+(s.trim().length>130?"...":""),reason:`Surcharge (${len} mots). Accumulation de propositions.`,suggestion:"Scindez pour aérer le rythme."});}}});const cleanWords=words.map(w=>w.toLowerCase().replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g,""));const ttr=(new Set(cleanWords).size/(wordCount||1))*100;const adverbs=(text.match(/\b\w+ment\b/gi)||[]).length;const adverbRatio=(adverbs/(wordCount||1))*100;const weakVerbs=(text.match(/\b(faire|fait|faisant|fais|avoir|a|as|ont|avez|dire|dit|être|est|suis|sommes|sont|aller|va|vais)\b/gi)||[]).length;const actionCoef=Math.max(10,Math.min(95,Math.round(75-(weakVerbs/(wordCount||1))*100+(shortSentences*0.8))));const clichesDb=[{regex:/un silence de mort/i,expr:"un silence de mort",alt:"un calme sépulcral, une absence de vibration"},{regex:/les larmes aux yeux/i,expr:"les larmes aux yeux",alt:"le regard brillant, la vue brouillée"},{regex:/battre la chamade/i,expr:"battre la chamade",alt:"s'emballer sauvagement"},{regex:/perdu dans ses pensées/i,expr:"perdu dans ses pensées",alt:"le regard ancré dans le vide"},{regex:/blanc comme un linge/i,expr:"blanc comme un linge",alt:"le teint livide, les traits blêmes"}];const clichesDetected=[];clichesDb.forEach(item=>{if(item.regex.test(text)){clichesDetected.push({expression:item.expr,alternative:item.alt});}});let rhythmLabel="Spectre synesthésique équilibré.";if(longSentences/sentenceCount>0.3)rhythmLabel="Teinte harmonique contemplative. Prose à ondes amples.";else if(shortSentences/sentenceCount>0.4)rhythmLabel="Teinte harmonique incandescente et staccato.";const actionPlan=[{target:"Stylistique",priority:adverbRatio>2.5?"Haute":"Modérée",instruction:adverbRatio>2.5?"Réduisez les adverbes en -ment pour dynamiser la prose.":"Densité adverbiale sous contrôle."},{target:"Structure",priority:heavyPhrases.length>2?"Haute":"Basse",instruction:heavyPhrases.length>2?"Scindez vos phrases de plus de 25 mots pour éviter l'érosion narrative.":"Bonne gestion des respirations syntactiques."},{target:"Vocabulaire",priority:ttr<45?"Haute":"Basse",instruction:ttr<45?"Enrichissez le lexique (synonymes des verbes ternes).":"Diversité lexicale satisfaisante."}];return NextResponse.json({metrics:{hookScore:Math.max(15,Math.min(98,Math.round(65+(ttr*0.3)-(adverbRatio*4)))),rhythmStyle:rhythmLabel,adverbDensity:adverbRatio>2.5?"Pression modérée. Traquez les tics.":"Optimale. Équilibre parfait.",vocabularyRichness:Math.round(ttr),readingTime:Math.max(1,NextResponse.json,Math.ceil(wordCount/200)),dynamicCoefficient:actionCoef,weakVerbsCount:weakVerbs},clichesDetected:clichesDetected,heavyPhrases:heavyPhrases,actionPlan:actionPlan});}catch(e){return NextResponse.json({error:"Erreur interne d'extraction."},{status:500});}}
